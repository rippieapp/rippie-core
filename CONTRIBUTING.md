# Contributing

Rippie Core is being prepared for its first public implementation. Implementation pull requests
are not open yet.

You can open an issue to discuss a provider integration, expected API shape, cache behavior, or an
example that would make the package more useful. Keep proposals focused on reusable core
functionality.
